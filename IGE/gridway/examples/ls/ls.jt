EXECUTABLE = /bin/ls
ARGUMENTS = -la
STDIN_FILE  = /dev/null
STDOUT_FILE = ls.out.${JOB_ID}
STDERR_FILE = ls.err.${JOB_ID}
#REQUIREMENTS = (ARCH = "i686" | ARCH="x86") & CPU_MHZ > 1000
#REQUIREMENTS = HOSTNAME = "ce02.ific.uv.es"
RANK = (CPU_MHZ * 2) + FREE_MEM_MB
